# 1. Configuracoes Gerais
# -----------------------
# Modo debug (True para desenvolvimento, False para produção)
DEBUG=True

# Chave secreta para JWT - GERE UMA NOVA CHAVE PARA PRODUÇÃO!
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GERE_UMA_CHAVE_SEGURA_AQUI

# 2. Banco de Dados (PostgreSQL)
# ------------------------------
# Formato: postgresql://usuario:senha@host:porta/nome_do_banco
DATABASE_URL=postgresql://lia:SUA_SENHA_SEGURA@localhost:5432/lia

# 3. Integracao IA (OpenRouter)
# ------------------------------
# Chave de API do OpenRouter (obrigatória)
OPENROUTER_API_KEY=sua_chave_openrouter_aqui

# Modelo padrão (opcional)
# OPENROUTER_DEFAULT_MODEL=arcee-ai/trinity-mini:free

# 4. Dados
# --------
# Caminho do CSV do PAC
PAC_CSV_PATH=app/data/DETALHAMENTOS.csv

# 5. Configurações de Produção (opcional)
# ---------------------------------------
# URLs permitidas para CORS (separadas por vírgula)
# CORS_ORIGINS=https://lia.tre-go.jus.br,https://api.tre-go.jus.br

# URL pública da API
# API_PUBLIC_URL=https://lia.tre-go.jus.br
